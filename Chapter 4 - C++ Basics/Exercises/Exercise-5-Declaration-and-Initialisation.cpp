#include <iostream>
using namespace std;


int main()
{
    int  a = 8; // Declaring an int variable with a value of 8 as an initialsation
    int b = 10; // Declaring an int variable with a value of 10 as an initialisation
    int sum; // Declaring an int variable that sums the answer between variables a and b

    sum = a + b; // Formula for the sum

    cout << sum; // Outputs the sum

    cin.get(); // Keeps the console window open
    return 0;

}

